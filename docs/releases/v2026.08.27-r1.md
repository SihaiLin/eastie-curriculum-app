# v2026.08.27-r1

**Status:** Production baseline after clean repo rebuild  
**Date:** 2026-08-27

## Scope

- Clean repo version deployed from `/Users/Lucia/Desktop/eastie_curriculum_app/`.
- Email-code login is active for first-time teacher access.
- Teacher personal password setup and future email/password login are active.
- Curriculum dashboard is active.
- Teacher feedback workflow is active.
- HTTPS has been restored for `eastie.sihai.space`.
- Frontend fixes for short network disconnects and password/email-code copy are included in the production baseline.

## Known Notes

- Resources are synced as scoped server resources, not stored fully in Git.
- Generated TypeScript curriculum snapshots are runtime snapshots, not hand-edited source.
- The old project at `/Users/Lucia/Desktop/eastie_curriculum_project/` remains historical reference only.
- The large `web/public/curriculum-resources/` tree is intentionally outside the Git baseline.
- `curriculum_release/` is not part of the initial clean app repo baseline.

## Verification Summary

- Login: production email-code and password-login flow is active; unauthenticated `set-password` access returns 401.
- Curriculum: `/curriculum/` loads from the rebuilt app.
- Key routes: representative K1/K2/K3 Unit 1 curriculum routes were verified during deployment checks.
- Feedback: teacher feedback workflow remains part of the deployed API surface.
- Resources: representative Unit 1 RAZ PDF and Power Up manifest URLs return 200; resources remain scoped on the server.
- HTTPS: `/login`, `/curriculum/`, and `/api/health` are served over restored HTTPS.

## Next Release Rule

Feedback-based fixes should become the next `vYYYY.MM.DD-rN` release after they are committed, deployed, and verified.

