# Web Rules

Place frontend and web integration rules here.

Examples:

- curriculum data contract rules;
- content sync rules;
- route and navigation rules;
- rendering and bilingual display rules;
- mobile behaviour rules;
- feedback UI rules.

If a rule is still only a reference document under `web/docs/`, register that reference in `../RULES_INDEX.md` before copying or consolidating it here.

