# API Rules

Place backend and API rules here.

Examples:

- auth and session rules;
- user role rules;
- feedback API rules;
- cookie, CORS, and deployment security rules;
- database migration rules.

If a rule is still only a reference document under `api/docs/`, register that reference in `../RULES_INDEX.md` before copying or consolidating it here.

